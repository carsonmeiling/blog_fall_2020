<h1>All Blog Posts</h1>

<%= link_to 'Create new blog', '/pages/new'%>


<% @pages.each do |page| %>
  <div>
  <%= page.title %>
  <%= page.author %>
  <%= link_to 'Blog', page_path(page) %>
  <%= link_to 'Destroy Page', page , method: :delete%>
  </div>

<% end %>